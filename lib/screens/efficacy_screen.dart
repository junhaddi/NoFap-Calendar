import 'package:flutter/material.dart';

class EfficacyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('금딸효능'),
      ),
    );
  }
}
